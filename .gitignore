node_modules/
npm-debug.log*

# Real credentials. .env.example is the template that is committed.
.env
.env.*
!.env.example

# Fetched from the API at build or run time; the bot also refreshes it at boot.
data/

*.log
.DS_Store
